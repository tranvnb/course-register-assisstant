const User = require('./user');
const { Course, Schedule } = require('./course');
const UserTimetable = require('./timetable');

module.exports = {
    User,
    Course,
    Schedule,
    UserTimetable
}
